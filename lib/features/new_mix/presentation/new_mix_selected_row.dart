import 'package:flutter/material.dart';

class NewMixSelectedRow extends StatelessWidget {
  const NewMixSelectedRow({super.key});

  @override
  Widget build(BuildContext context) {
    return const Row(
      children: [],
    );
  }
}
