import 'package:flutter/material.dart';

class MixCollections extends StatelessWidget {
  const MixCollections({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
