import 'package:flutter/material.dart';

class SleepModeAdd extends StatelessWidget {
  const SleepModeAdd({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
